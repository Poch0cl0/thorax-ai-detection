# API

La documentación interactiva OpenAPI está disponible en el backend en `/docs` y `/redoc` (esquema en `/openapi.json`).

En el stack Docker con Nginx, el mismo esquema y Swagger UI se exponen a través del proxy en la raíz del host (p. ej. `http://localhost/docs`).
