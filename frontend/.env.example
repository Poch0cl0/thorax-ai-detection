# URL del API FastAPI (vacío = mismo origen en producción; en dev es http://127.0.0.1:8000)
# VITE_API_BASE_URL=http://127.0.0.1:8000

# Datos clínicos desde JSON demo (sesión + sessionStorage)
VITE_USE_CLINICAL_MOCK=true

# Omite login real: entra con el correo del formulario (solo desarrollo)
# VITE_SKIP_AUTH=true

# Con SKIP_AUTH y mock, fuerza correo inicial
# VITE_DEMO_LOGIN_EMAIL=demo@hospital.com
